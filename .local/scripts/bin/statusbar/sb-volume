#!/bin/sh

volume=$(pamixer --get-volume)

symbol="墳"

[ "$volume" -le 25 ] && symbol="奔"
[ "$volume" -ge 80 ] && symbol=""
[ "$volume" = "0" ] && symbol="婢"

printf "^c%s^%s %s%%" "$1" "$symbol" "$volume"

case $BUTTON in
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac
