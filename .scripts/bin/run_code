#!/bin/bash

output="$(compile "$1")"

BIN_FILE="${output%§*}"
EXEC_CMD="${output#*§}"

[ -z "$EXEC_CMD" ] && exit 1

printf "\$ %s\n\n" "$EXEC_CMD"

time $EXEC_CMD ; printf "\nPress any key to exit..." && wait_key_press

[ -n "$BIN_FILE" ] && rm -rf $BIN_FILE
