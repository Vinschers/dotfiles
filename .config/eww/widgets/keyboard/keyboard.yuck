(deflisten keyboard "scripts/keyboard.sh")

(defwidget keyboard []
  (custom_widget

    (box
      :class "keyboard"
      :space-evenly false

      (label
        :class "keyboard-icon"
        :text "󰌌"
      )

      (label
        :class "keyboard-layout"
        :text "${keyboard}"
      )
    )
  )
)
