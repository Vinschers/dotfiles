(deflisten keyboard "scripts/keyboard.sh")

(defwidget keyboard []
  (box
    :class "keyboard"
    :space-evenly false

    (label
      :class "keyboard-icon"
      :text "󰌌"
    )

    (label
      :class "keyboard-layout"
      :text "${keyboard}"
    )
  )
)
