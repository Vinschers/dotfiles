(defvar music_reveal false)

(defwidget music_widget []
  (box
    :class "${music != "" ? "music" : ""}"
    :space-evenly false

    (label
      :class "spotify-logo"
      :text "${music != "" ? "" : ""}"
    )
    (label
      :class "song-title"
      :text "${music.title}"
    )
  )
)
